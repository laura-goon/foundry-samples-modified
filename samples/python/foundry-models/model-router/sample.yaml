name: Model Router Quickstart with Chat Completions API
description: Basic quickstart sample demonstrating the use of Model Router with the Chat Completions API.

build: "pip install -r requirements.txt"
validate: "python -m py_compile *.py"