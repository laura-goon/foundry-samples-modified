name: Web Server with Vision Responses (Python)
description: Vision (image understanding) using the Foundry Local web service and OpenAI Responses API.

build: "pip install -r requirements.txt"
validate: "python -m py_compile $(find . -maxdepth 3 -name '*.py' -not -path './.venv/*')"
