name: LangChain Integration (Python)
description: LangChain integration for building translation and text generation chains.

build: "pip install -r requirements.txt"
validate: "python -m py_compile $(find . -maxdepth 3 -name '*.py' -not -path './.venv/*')"
