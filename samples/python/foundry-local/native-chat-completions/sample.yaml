name: Native Chat Completions (Python)
description: Initialize the SDK, start the local service, and run streaming chat completions.

build: "pip install -r requirements.txt"
validate: "python -m py_compile $(find . -maxdepth 3 -name '*.py' -not -path './.venv/*')"
