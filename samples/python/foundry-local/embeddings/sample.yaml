name: Embeddings (Python)
description: Generate single and batch text embeddings using the Foundry Local SDK.

build: "pip install -r requirements.txt"
validate: "python -m py_compile $(find . -maxdepth 3 -name '*.py' -not -path './.venv/*')"
