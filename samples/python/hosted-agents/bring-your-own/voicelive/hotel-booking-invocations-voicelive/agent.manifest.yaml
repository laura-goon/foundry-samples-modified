# yaml-language-server: $schema=https://raw.githubusercontent.com/microsoft/AgentSchema/refs/heads/main/schemas/v1.0/AgentManifest.yaml
name: hotel-booking-python-invocations-voicelive
displayName: "Hotel Booking Agent"
description: >
  A hotel booking agent demonstrating voice + click interactions.
  Handles natural language hotel search via voice and hotel selection /
  booking confirmation via button clicks. Designed for Voice Live SDK
  multi-modal scenarios.
metadata:
  voiceLiveCompatible: "true"
  tags:
    - AI Agent Hosting
    - Azure AI AgentServer
    - Invocations Protocol
    - Streaming
    - Voice + Click
template:
  name: hotel-booking-python-invocations-voicelive
  kind: hosted
  protocols:
    - protocol: invocations
      version: 1.0.0
  environment_variables:
    # FOUNDRY_PROJECT_ENDPOINT and APPLICATIONINSIGHTS_CONNECTION_STRING
    # are injected by the platform (hosted) and translated by azd (local)
    # — do NOT declare them here.
    #
    # Model deployment name — resolved from the resources section below.
    - name: AZURE_AI_MODEL_DEPLOYMENT_NAME
      value: "{{AZURE_AI_MODEL_DEPLOYMENT_NAME}}"
resources:
  - kind: model
    id: gpt-4.1-mini
    name: AZURE_AI_MODEL_DEPLOYMENT_NAME
