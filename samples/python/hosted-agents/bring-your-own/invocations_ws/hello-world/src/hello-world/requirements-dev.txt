# Local-only dev/test dependencies.
#
# Not installed inside the hosted container (Dockerfile installs requirements.txt only).
# Required for running the bundled WebSocket test client and the browser proxy locally:
#   - e2e_local.py
#   - chat_client/proxy.py
#
# Install with: pip install -r requirements-dev.txt

# Pinned to >=14 for the websockets.connect(additional_headers=...) kwarg used
# when sending the Foundry Authorization header to a deployed agent.
websockets>=14
