python-dotenv
uvicorn
fastapi
pipecat-ai[silero,webrtc,websocket,azure]>=0.0.105
pipecat-ai-subagents>=0.3.0
azure-ai-transcription
azure-identity
httpx
# pipecat[webrtc] depends on opencv-python (GUI variant), which requires X11
# libs (libxcb1, libGL, ...) that aren't present in python:slim. Pin the
# headless wheel here so pip won't install the GUI variant.
opencv-python-headless

# debugpy enables local debugging of this agent with the Foundry Toolkit VS Code extension.
debugpy
