FOUNDRY_PROJECT_ENDPOINT="..."
AZURE_AI_MODEL_DEPLOYMENT_NAME="..."
# Full MCP endpoint of the toolbox the agent consumes, e.g.
# https://<account>.services.ai.azure.com/api/projects/<project>/toolboxes/agent-tools/mcp?api-version=v1
TOOLBOX_ENDPOINT="..."

# Only needed when running provision_kb.py to create the knowledge base:
AZURE_SEARCH_ENDPOINT="https://<your-search>.search.windows.net"
AZURE_OPENAI_ENDPOINT="https://<account>.openai.azure.com"
