name: Verify WinML Execution Providers (JavaScript)
description: Verify WinML 2.0 execution providers are correctly registered.

# SKIP: Windows-only: requires the foundry-local-sdk-winml package.
build: "echo 'SKIP: Windows-only: requires the foundry-local-sdk-winml package.'"
validate: "echo 'SKIP: Windows-only: requires the foundry-local-sdk-winml package.'"
