name: Native Chat Completions (JavaScript)
description: Initialize the SDK, download a model, and run non-streaming and streaming chat completions.

build: "npm install --omit=optional --no-audit --no-fund"
