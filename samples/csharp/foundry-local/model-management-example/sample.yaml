name: Model Management (C#)
description: Manage models, variant selection, and updates with the Foundry Local C# SDK.

build: "dotnet restore --source https://api.nuget.org/v3/index.json && dotnet build --no-restore --verbosity minimal"
